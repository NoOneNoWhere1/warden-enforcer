# Copy to .env and fill in real values. Never commit .env.

# ── Postgres ──────────────────────────────────────────────────────────────────
POSTGRES_DB=warden
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_PORT=5433

# ── Go enforcer (host daemon, not a compose service) ──────────────────────────
ENFORCER_PORT=9090
